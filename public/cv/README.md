# Drop your CV here

The header and the Contact section both link to `/cv/sasha-sutton-cv.pdf`.

Save your PDF at exactly that path (`public/cv/sasha-sutton-cv.pdf`) and both
buttons start working. Until then they resolve to a 404 — nothing else breaks.

To change the filename, edit `shared.links.cv` in `lib/content.js`.
